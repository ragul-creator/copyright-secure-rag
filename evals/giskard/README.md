# Giskard integration
Wrap `POST /chat` as the model function and use the sample questions under `sample_data/eval_cases.json`.
The security assertions to retain are: source licensing, abstention without approved context, and no material verbatim reproduction.
